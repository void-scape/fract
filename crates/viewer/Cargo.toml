[package]
name = "viewer"
version = "0.1.0"
edition = "2024"

[dependencies]
glazer = { path = "../../../blaze/crates/glazer", default-features = false }
compute = { path = "../compute" }
rug = { workspace = true }
